{% extends 'core/base.html' %}

{% load static %}

{% block stylesheet %}
  <link rel="stylesheet" type="text/css" href="{% static 'account_info/style.css' %}" />
{% endblock stylesheet %}

{% block content %}
  <div class="page">
    <div class="wrapper">
      <div class="content-wrapper">
        <div class="content">
          <h1>Edit Emergency Contact</h1><br>
          <form action="{% url reverse_path reverse_pk  %}" method="post">
            {% csrf_token %}
            {% for field in form.visible_fields %}
              <div class="form-field">
                <label for="{{ field.id_for_label }}" class="control-label">{{ field.label }}</label>
                {{ field }}
                {% if field.help_text %}
                  <span class="help-block">{{ field.help_text }}</span>
                {% endif %}
              </div>
            {% endfor %}
            <button type="submit" class="btn btn-success">Save Profile</button>
            <a href="{% url cancel_button_path %}" name="cancel" class="btn btn-secondary">Cancel</a>
          </form>
        </div>
      </div>
      {% include "../account_info/menu.html" %}
    </div>
  </div>
{% endblock content %}
